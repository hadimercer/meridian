# Meridian pipeline package
# Modules:
#   db.py       — Supabase connection and query helpers
#   auth.py     — Session management and role helpers
#   scoring.py  — RAG scoring engine (core logic)
#   invite.py   — Invite link generation and token resolution
